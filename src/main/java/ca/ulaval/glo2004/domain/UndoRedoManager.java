/*
 * Click nbfs://nbhost/SystemFileSystem/Templates/Licenses/license-default.txt to change this license
 * Click nbfs://nbhost/SystemFileSystem/Templates/Classes/Class.java to edit this template
 */

package ca.ulaval.glo2004.domain;



public class UndoRedoManager {
    /*
    
    
private Stack<Chalet> undos = new Stack<>();
private Stack<Chalet> redos = new Stack<>();

public void save(Chalet chalet) {
    undos.push(chalet);
    redos.clear();  // Clear the redo stack when a new state is saved
}

public Chalet undo() {
    if (!undos.isEmpty()) {
        // Pop the top state from the undo stack
        Chalet undoneState = undos.pop();

        // Push the current state to the redo stack
        redos.push(currentState);

        // Apply the undone state
        currentState = undoneState;

        // Return the object's state after the undo
        return currentState;
    }

    // If there's nothing to undo, return the current state
    return currentState;
}
*/
}
